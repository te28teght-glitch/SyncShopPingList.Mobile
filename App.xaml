<?xml version="1.0" encoding="utf-8" ?>
<Application xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SyncShoppingList.Mobile.App">
    
    <Application.Resources>
        <ResourceDictionary>
            
            <!-- Цветовая схема -->
            <Color x:Key="PrimaryColor">#2E4A7A</Color>
            <Color x:Key="PrimaryDarkColor">#1C2F4F</Color>
            <Color x:Key="SecondaryColor">#FF6B6B</Color>
            <Color x:Key="BackgroundColor">#1E1E1E</Color>
            <Color x:Key="SurfaceColor">#2D2D2D</Color>
            <Color x:Key="TextColor">#FFFFFF</Color>
            <Color x:Key="PlaceholderColor">#888888</Color>
            
            <!-- Стиль для кнопок -->
            <Style TargetType="Button">
                <Setter Property="BackgroundColor" Value="{StaticResource PrimaryColor}"/>
                <Setter Property="TextColor" Value="{StaticResource TextColor}"/>
                <Setter Property="CornerRadius" Value="25"/>
                <Setter Property="HeightRequest" Value="50"/>
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="FontAttributes" Value="Bold"/>
            </Style>
            
            <!-- Стиль для полей ввода -->
            <Style TargetType="Entry">
                <Setter Property="BackgroundColor" Value="{StaticResource SurfaceColor}"/>
                <Setter Property="TextColor" Value="{StaticResource TextColor}"/>
                <Setter Property="PlaceholderColor" Value="{StaticResource PlaceholderColor}"/>
                <Setter Property="HeightRequest" Value="50"/>
                <Setter Property="FontSize" Value="15"/>
            </Style>
            
            <!-- Стиль для заголовков -->
            <Style TargetType="Label" x:Key="TitleLabel">
                <Setter Property="FontSize" Value="28"/>
                <Setter Property="FontAttributes" Value="Bold"/>
                <Setter Property="TextColor" Value="{StaticResource TextColor}"/>
            </Style>
            
            <Style TargetType="Label" x:Key="SubtitleLabel">
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="TextColor" Value="{StaticResource PlaceholderColor}"/>
            </Style>
            
        </ResourceDictionary>
    </Application.Resources>
    
</Application>